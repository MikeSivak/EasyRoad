import Home from '../components/Home/Home.js'

const routes = [
    {
        path: '/',
        name: 'Home',
        component: Home,
        exact: true,
        id: 0
    },
]

export default routes;